server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location = /timeline {
        default_type text/html;
        try_files /timeline.html =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
